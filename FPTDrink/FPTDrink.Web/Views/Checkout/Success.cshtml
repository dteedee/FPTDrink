@{
	ViewData["Title"] = "Đặt hàng thành công";
	var orderCode = (string?)ViewBag.OrderCode ?? "";
}

<div class="text-center py-5">
	<div class="display-6 text-success mb-3">🎉 Cảm ơn bạn!</div>
	<p class="lead">Đơn hàng của bạn đã được tạo thành công.</p>
	@if (!string.IsNullOrWhiteSpace(orderCode))
	{
		<p class="mt-3">Mã đơn của bạn: <span class="badge bg-primary">@orderCode</span></p>
	}
	<div class="mt-4 d-flex gap-2 justify-content-center">
		<a class="btn btn-outline-primary" asp-controller="Home" asp-action="Index">Tiếp tục mua sắm</a>
		<a class="btn btn-primary" asp-controller="TraCuuDonHang" asp-action="Index">Tra cứu đơn hàng</a>
	</div>
}

